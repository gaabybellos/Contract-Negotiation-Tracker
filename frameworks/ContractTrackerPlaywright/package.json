{
  "name": "contract-tracker-playwright",
  "version": "1.0.0",
  "description": "Standalone E2E test repository for Contract Negotiation Tracker",
  "private": true,
  "scripts": {
    "test": "playwright test",
    "test:ui": "playwright test --ui",
    "report": "playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "^1.42.0",
    "@types/node": "^20.0.0"
  }
}
